# Skill Registry — everyone_web

Generated by `sdd-init`. Used by sub-agents during SDD phases to inject relevant project standards.

## Project Conventions

No project-level convention files detected (`AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`).
Project standards derive from the user-level CLAUDE.md and the user skills below.

## User Skills

| Skill | Triggers (codes / contexts) | Path |
|-------|-----------------------------|------|
| branch-pr | Creating a PR, opening a pull request, preparing changes for review | ~/.claude/skills/branch-pr/SKILL.md |
| issue-creation | Creating a GitHub issue, bug report, feature request | ~/.claude/skills/issue-creation/SKILL.md |
| judgment-day | Adversarial dual review ("juzgar", "judgment day", "doble review") | ~/.claude/skills/judgment-day/SKILL.md |
| skill-creator | Creating new AI agent skills | ~/.claude/skills/skill-creator/SKILL.md |
| go-testing | Writing Go tests, Bubbletea TUI testing | ~/.claude/skills/go-testing/SKILL.md |

> `go-testing` is listed for completeness but does NOT apply to this React/TypeScript project.

## Compact Rules (auto-resolved into sub-agent prompts)

### Global Rules (from ~/.claude/CLAUDE.md)
- **Conventional commits only** — never add "Co-Authored-By" or AI attribution.
- **Never build after changes** — do NOT run `npm run build` proactively.
- **Use `bat` / `rg` / `fd` / `sd` / `eza`** — never `cat`, `grep`, `find`, `sed`, `ls`.
- **Verify before agreeing** — challenge claims with code/docs, not assumptions.
- **Concepts > code** — explain WHY when correcting; back claims with technical reasoning.
- **Match user language** — Peninsular Spanish (tuteo + vosotros) when user writes in Spanish; English otherwise.

### React + TanStack Start Rules
- **Stack**: React 19, TanStack Start (SSR), TanStack Router (file-based), TanStack Query, Vite 7, TypeScript 5.9 strict.
- **Path aliases**: import from `@everyone-web/*` (maps to `./src/*`). The `@/` alias also resolves to `./src` via Vite. Prefer `@everyone-web/*` per `tsconfig.json`.
- **Routes**: file-based under `src/routes/`. Do NOT hand-edit `src/routeTree.gen.ts` — TanStack regenerates it.
- **Components folder layout**: feature components under `src/components/<feature>/`, layouts under `src/layouts/<feature>/`, ui primitives under `src/ui/` (shadcn/ui — new-york style, neutral base, Lucide icons).
- **Data fetching**: TanStack Query for client state, server-side via TanStack Start loaders/`createServerFn` when applicable.
- **Styling**: Tailwind v4 (CSS-first config in `src/styles.css`), `clsx` + `tailwind-merge` via `cn()` helper in `src/libs/utils.ts`.
- **Supabase**: client lives in `src/libs/supabase.ts`; env vars via `src/constants/dotenv.constants.ts`.
- **TS**: `strict: true`, `noUnusedLocals`, `noUnusedParameters`, `verbatimModuleSyntax: true` — be explicit with `import type`.

### Testing Rules (Strict TDD enabled)
- **Test runner**: `npm run test` (vitest run). Single-test runs: `npx vitest run path/to/file.test.tsx`.
- **Layers**: unit + component (vitest + @testing-library/react + jsdom). No E2E configured.
- **TDD cycle**: Red → Green → Refactor. Write the failing test FIRST. Do NOT write production code without a failing test.
- **No coverage tooling installed** — do not assume `--coverage` works.

### Quality Rules
- **Lint**: `npm run lint` (eslint flat config + @typescript-eslint + react + jsx-a11y + prettier).
- **Typecheck**: `npx tsc --noEmit` (the `build` script does `vite build && tsc`, but per global rule do NOT run build).
- **Format**: prettier (config in `.prettierrc`).

### Workflow Rules
- **PR creation** → use `branch-pr` skill. Issue first, then branch, then PR.
- **Issue creation** → use `issue-creation` skill.
- **Adversarial review** when user asks for "judgment-day" / "juzgar" → `judgment-day` skill.

## Trigger Map (which skill to inject for which sub-agent task)

| Sub-agent task | Inject |
|----------------|--------|
| Reads/edits `src/**/*.{ts,tsx}` | React + TanStack rules, Testing rules, Quality rules, Global rules |
| Writes tests | Testing rules + React rules + Global rules |
| Creates a PR | branch-pr compact rules + Global rules |
| Creates an issue | issue-creation compact rules + Global rules |
| Adversarial review | judgment-day compact rules |
| Creates a new skill | skill-creator compact rules |
